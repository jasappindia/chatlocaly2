package com.chatlocaly.adapter;

/**
 * Created by ashok on 31/3/17.
 */

public class CreditCardYearList  {



    /*Context context;
    int layoutResourceId;
    List<VehicleMakeListModel.MakeList> data;

    public CreditCardYearList(Context context, int layoutResourceId, List<VehicleMakeListModel.MakeList> data) {
        super(context, layoutResourceId, data);
        this.layoutResourceId = layoutResourceId;
        this.context = context;this.data = data;
    }
    @Override
    public View getDropDownView(int position, View convertView,
                                ViewGroup parent) {
        // TODO Auto-generated method stub
        return getView(position,convertView,parent);
    }
    @Override
    public View getView(int position, View convertView, ViewGroup parent) {
        View row = convertView;
        VehicleMakeListAdapter.WeatherHolder holder = null;

        if (row == null) {
            LayoutInflater inflater = ((Activity) context).getLayoutInflater();
            row = inflater.inflate(layoutResourceId, parent, false);
            holder = new VehicleMakeListAdapter.WeatherHolder();
            holder.txtTitle = (TextView) row.findViewById(R.id.tv_search_row);
            row.setTag(holder);
        } else {
            holder = (VehicleMakeListAdapter.WeatherHolder) row.getTag();
        }

        //   data.get(position).getName();
        holder.txtTitle.setText(data.get(position).getName());


        return row;
    }

    static class WeatherHolder {

        TextView txtTitle;
    }
*/

}
